import { describe, it, expect } from 'vitest'
import { handleGlobalPrSearch } from './handle-global-search.js'

describe('handleGlobalPrSearch', () => {
  it('should be a function', () => {
    expect(typeof handleGlobalPrSearch).toBe('function')
  })
})
