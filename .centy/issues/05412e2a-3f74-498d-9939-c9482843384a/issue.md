# Project path format

When a project is inside a user path show `~/` instead of the user section, make sure it's working with each OS, Use lib if existing, if not raise a flag and do not continue with this task
