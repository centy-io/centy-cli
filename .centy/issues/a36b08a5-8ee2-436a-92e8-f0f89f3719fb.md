---
displayNumber: 188
status: open
priority: 2
createdAt: 2026-02-15T20:53:39.804126+00:00
updatedAt: 2026-02-15T20:53:39.804126+00:00
---

# Add CLI command for creating custom item types

The daemon now supports a CreateItemType gRPC endpoint (centy-io/centy-daemon#66) that allows creating custom item types dynamically with validation, disk persistence, and manifest updates. The CLI needs a corresponding command to expose this functionality.

## Background

The CreateItemType RPC accepts:

- name - Singular display name (e.g., “Bug”, “Task”, “Epic”)
- plural - Folder/type key, lowercase alphanumeric + hyphens (e.g., “bugs”, “tasks”, “epics”)
- identifier - “uuid” or “slug”
- features - Toggle-able features (display_number, status, priority, assets, org_sync, move, duplicate)
- statuses - Allowed statuses (e.g., \[“open”, “in-progress”, “closed”\])
- default_status - Default status for new items (must be in statuses list)
- priority_levels - Number of priority levels (0 = none)
- custom_fields - Custom field definitions

## CLI command

```bash
centy create item-type --name "Bug" --plural "bugs" --identifier uuid \
  --statuses open,in-progress,closed --default-status open \
  --priority-levels 3 \
  --features display-number,status,priority,move,duplicate
```

## Files to create/modify

### New files

- src/daemon/daemon-create-item-type.ts - gRPC client wrapper (like daemon-create-issue.ts)
- src/commands/create/item-type.ts - CLI command
- src/daemon/types/item-type.ts - TypeScript types for CreateItemTypeRequest, CreateItemTypeResponse, ItemTypeFeatures, ItemTypeConfigProto

### Modified files

- src/daemon/grpc-client-items.ts - Add createItemType method to CentyDaemonItemsClient
- src/daemon/types.ts - Re-export new types from types/item-type.ts

## Notes

- Follow the existing pattern in daemon-create-issue.ts / create/issue.ts for the gRPC client wrapper and oclif command structure
- The features flag should accept a comma-separated list of feature names
- The statuses flag should accept a comma-separated list
- Validate that default_status is in the statuses list on the CLI side for fast feedback
- Proto types need to be generated from the updated proto (after daemon PR #66 lands)
- Reference: centy-io/centy-daemon#66
