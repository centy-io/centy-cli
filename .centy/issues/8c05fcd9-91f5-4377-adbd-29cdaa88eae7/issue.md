# Add shorthand aliases for common commands

Add intuitive shorthand commands as aliases. For example: 'centy issue <id>' as an alias for 'centy get issue <id>'. This makes the CLI more discoverable and faster to use.
