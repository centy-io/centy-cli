# Desktop installer should automatically install the daemon

The desktop application installer should bundle and install the centy-daemon alongside the desktop app, so users don't need to install them separately.
