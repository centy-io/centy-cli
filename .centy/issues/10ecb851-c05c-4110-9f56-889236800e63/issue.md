# Add --project flag to specify target project

Add a --project flag to commands like 'centy get issue' to specify which project to query without needing to change directories. Example: 'centy get issue <id> --project centy-daemon'
