# Missing daemon

When the Demon is missing from the file system, notice that the user needs to install the Demon so the CL I will be able to work with it and let the user to have an option of auto installed for the CLI
